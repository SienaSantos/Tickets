# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Tickets

GET /tickets 							            controllers.TicketController.tickets()
GET /tickets 							            controllers.TicketController.editTicket(id: Long)
POST /tickets 						            controllers.TicketController.newTicket()
POST /tickets/:id/delete              controllers.TicketController.deleteTicket(id: Long)
GET /tickets/:id/show                 controllers.TicketController.displayTicket(id: Long)
POST /tickets/:id/update              controllers.TicketController.updateTicket(id: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
